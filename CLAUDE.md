# CLAUDE.md — The Alumni Center

> This is the living operational reference for this repo. **Read this file first, every session.** `DESIGN.md` is the historical brainstorming record that produced these decisions — it's kept for context but this file is what governs day-to-day work. Any change to architecture, conventions, or the standing rules below gets reflected here in the same commit as the change that caused it, never as a follow-up.

---

## 1. Standing rules (permanent — apply to every session, every commit)

These are not one-time setup tasks. Re-read and follow them every session.

1. **`CLAUDE.md` maintained from commit one.** This file. Keep it current with architecture, conventions, ledger invariants — everything a fresh Claude Code session needs without prior chat history.
2. **Transferability is a top priority, on par with correctness.** This system must be handoff-ready — new GitHub org, new Railway account, new Vercel account, zero shared history — at any point, not just at some future "handoff phase."
3. **`README.md`** must let a new developer with nothing but repo access get the full stack running locally, top to bottom. Update it the same commit any setup step changes.
4. **No hardcoded secrets or account-specific values, ever.** No API keys, Stripe IDs, Railway/Vercel project refs, phone numbers, or domain names in code. Everything account-specific comes from env vars. `.env.example` gets a new var in the same commit that introduces the code reading it, with a comment on what it is and where to get it.
5. **Schema is fully reproducible from migrations, never hand-edited.** No manual schema changes via a GUI. Fresh Postgres + `pnpm db:migrate` + `pnpm db:seed` must produce a working dev environment.
6. **`docs/DEPLOYMENT.md`** — exact steps to stand up the whole system on brand-new Railway/Vercel/Stripe/Twilio accounts. If it can't live in code, it lives here.
7. **`docs/HANDOFF.md`** — the transfer checklist: what changes hands, what credentials rotate, and the order to do it in.
8. **Nothing lives only in a dashboard.** Any Railway/Vercel/Stripe/Twilio dashboard config gets written into `docs/DEPLOYMENT.md` the same day it's configured.
9. **Transfer-blocker audit before every milestone** (end of each build-order phase, see §6 below). Check for: hardcoded values, undocumented dashboard config, stale `.env.example`, stale `CLAUDE.md`. Fix before moving on.
10. **Ask before making a product decision not already resolved in `DESIGN.md`.** Make technical implementation decisions independently and document them here (§4).

---

## 2. What this system is

A multi-sport athletic facility where members buy **tokens** (Dave & Buster's-style economy) and spend them across walk-ins, leagues, camps, free play, reservations, and lessons. Each participant (not account) carries a **wallet** and a QR-code digital membership card ("The Alumni Card") scanned at activity stations. Full spec: `DESIGN.md`.

**Brand:** The Alumni Center. Primary color `#0F5898`. Logo at [`brand/alumni-center-logo.png`](brand/alumni-center-logo.png). Collegiate/varsity visual direction.

---

## 3. Core architecture decisions (from DESIGN.md §3)

- **Ledger-based tokens.** Every purchase/redemption/refund/bonus/transfer/adjustment is an immutable row in `token_ledger`. Balance is always `SUM(amount)` for a participant — never a stored mutable counter.
- **Single write path.** Only [`apps/api/src/ledger/ledger-service.ts`](apps/api/src/ledger/ledger-service.ts) may insert into `token_ledger` or `points_ledger`. No other code path, migration, or script does this — the seed script calls `recordPurchase()` rather than inserting directly, and that's the pattern every future feature must follow.
- **Ledger rows are never updated or deleted.** Corrections are new offsetting rows (`refund`, `adjustment`). The ledger service exposes no update/delete functions by design (this is asserted in `ledger-service.test.ts`).
- **Per-participant wallets** (D&B Power Card model), not one pooled account wallet. Account view = display-only rollup across participants. Transfers between participants of the *same* account are free, instant, and produce paired net-zero rows.
- **All credit logic lives on the backend (Railway/`apps/api`).** Frontends are thin clients — they never compute or mutate balances.
- **Rotating QR tokens** (not yet implemented — Phase 3/4). QR encodes a short-lived signed token, never a raw member ID.
- **Concurrency safety:** ledger writes for a given participant are serialized within a transaction via `pg_advisory_xact_lock(hashtext(participant_id))`, so balance checks can't race with a concurrent write to the same participant.
- **No overlapping reservations per space**, enforced at the DB level via a Postgres `EXCLUDE USING gist` constraint (requires `btree_gist`) on `reservations` — not application code. See `packages/db/migrations/0001_reservation_no_overlap.sql`.
- **Every redemption has a beneficiary** (`house` / `vendor:{id}` / `coach:{id}`) via `token_ledger.beneficiary_partner_id` — this one column powers concessions, vendors, and coach payouts.
- **Loyalty points are a separate currency**, earned on token *spend* (not purchase), own append-only `points_ledger` mirroring the token ledger's invariants and single-write-path rule.
- **Sports are data, not code** — admin-managed via the `sports` table, no deploy needed to add one.
- **Offerings are the universal purchasable unit** (walk-in, free_play_pass, league, camp, reservation, lesson, clinic) — one shape for everything with a token cost.

---

## 4. Technical decisions made during build (not in DESIGN.md — decided here)

| Decision | Choice | Reasoning |
|---|---|---|
| Package manager | pnpm workspaces | Fast, disk-efficient, standard for TS monorepos |
| ORM | **Drizzle** (not Prisma) | Need raw-SQL-level control for the `EXCLUDE` constraint on `reservations` and advisory-lock ledger transactions; Drizzle's SQL-first model fits better than Prisma's abstraction layer for this kind of invariant-heavy schema |
| Backend framework | **Fastify** (not NestJS) | Lighter weight, less ceremony for a service that's mostly "ledger + REST," faster cold starts on Railway |
| Frontend framework | **Next.js everywhere** (resolves DESIGN.md open question #13) | Uniformity — one framework to reason about across marketing/web/admin/scan-station; scan-station's kiosk constraints don't need Vite's extra speed badly enough to justify a second toolchain |
| DB driver | `postgres` (postgres.js) via `drizzle-orm/postgres-js` | Simple, well-supported by Drizzle, works identically against Railway Postgres and local Homebrew Postgres |
| Test runner | Vitest | Fast, native ESM/TS support, no extra config needed for workspace `@alumni/*` packages |
| Local Postgres (dev) | Homebrew `postgresql@16`, no Docker | Docker wasn't available in the dev environment; `README.md` documents both paths |
| Calendar library (admin, Phase 2) | **Custom grid with `@dnd-kit/core`** (resolves DESIGN.md open question #21) | User chose this explicitly over FullCalendar's resource-timeline plugin (~$480/yr) to avoid a recurring vendor cost pre-revenue. See `apps/admin/app/(dashboard)/schedule/ScheduleGrid.tsx`. |
| Staff dashboard auth | Phone + password login → HS256 JWT (`jose`), stored in an httpOnly cookie set by a Next.js Server Action | Not specified in DESIGN.md (that schema's `kiosk_pin_hash` is for scan-station kiosk unlock, Phase 4, not dashboard login). Added `staff_users.password_hash` column (migration `0002_fat_vermin.sql`). Password hashing via Node's built-in `scrypt` (`apps/api/src/auth/password.ts`) — no extra dependency, adequate for staff-only login volume. |
| Admin↔API request pattern | **All API calls happen server-side** (Server Components / Server Actions in `apps/admin`), never from client-side JS | The browser only ever talks to the Next.js app; the staff JWT lives in an httpOnly cookie the client can't read. `apps/api` still has CORS configured (`ADMIN_APP_ORIGIN`) as defense-in-depth, not because it's relied on. |
| Standings formula | **Sport-specific** (resolves DESIGN.md open question #3) | User chose sport-specific over one generic formula. Basketball/pickleball: win% then point differential. Volleyball: win% then point *ratio* (not differential — matches how volleyball leagues actually break ties). Futsal: soccer-style league points (win=3/tie=1/loss=0) then goal differential. Unknown future sports fall back to the generic win%/differential formula. See `packages/shared/src/standings.ts`. |
| Team formation | **Captain self-serve** (resolves DESIGN.md open question #1) | Backend (`teams`/`team_members` endpoints) supports an optional `captainParticipantId` at creation and is ready for this now; the actual captain-facing "create team, invite by phone" UI belongs in the member PWA (Phase 3, needs phone-OTP participant auth that doesn't exist yet). Phase 2's admin dashboard has a staff-facing team/roster management view as an operational necessity in the meantime. |
| Score entry | **Staff only** (resolves DESIGN.md open question #2) | No dispute/correction workflow needed for v1. `POST /games/:id/score` requires staff auth; finalizing a score recalculates standings — see `apps/api/src/league/standings-service.ts`. |
| Enrollment capacity | **Enforced with waitlist** (resolves DESIGN.md open question #6) | `offerings.capacity` checked at enrollment time; over capacity → `status=waitlisted`, no token charge until staff promotes via `POST /enrollments/:id/promote`. See `apps/api/src/enrollments/enrollment-service.ts`. |
| Frontend stack (`apps/admin`) | Next.js 16 + React 19 + Tailwind CSS 3 | Started on Next 14.2.x/React 18 per the earlier framework decision. Bumping to Next 16/React 19 and building with `--webpack` instead of Turbopack fixed a real async-`cookies()`/`params` migration and a Turbopack-specific crash along the way. |
| Member auth (`apps/web`) | **Clerk** (phone-OTP), not raw Twilio Verify as DESIGN.md originally sketched | User's explicit call — Clerk's free tier covers this facility's expected volume and saves hand-rolling OTP delivery/session management/rate-limiting. Staff dashboard auth (Phase 2) is intentionally left as-is, not migrated to Clerk — no user-facing benefit, would be pure churn. |
| Member↔account linking | **Lazy get-or-create**, not a Clerk webhook | The first authenticated `/member/*` request from a new Clerk user creates (or links, by phone, if a staff-created guest account already exists) the `accounts`/owner-`participants` rows — see `apps/api/src/accounts/account-service.ts`. Simpler than standing up a Clerk webhook + svix verification for v1; `CLERK_WEBHOOK_SECRET` is reserved in `.env.example` if a real webhook is added later (e.g. to sync profile edits made in Clerk's hosted UI). |
| Member↔API request pattern | Same as admin — **all calls server-side** (Server Components/Actions in `apps/web`), authenticated with the caller's Clerk session token (`auth().getToken()`) | Consistent with `apps/admin`'s pattern (CS4 above); browser never sees a bearer token for our API. One exception: the QR-token refresh loop on the card page polls a same-origin Next.js Route Handler (`apps/web/app/api/qr-token/[participantId]/route.ts`) from the client, which itself calls our API server-side — the Clerk token still never reaches client JS. |
| Member-facing API namespace | All `apps/web`-facing routes live under `/member/*` on the same Fastify instance as the staff routes | `GET /offerings` already existed for staff (different auth, different shape) — same path, different prefix, avoids a route collision rather than needing a second deployed service. See `apps/api/src/server.ts`. |
| QR signing | HS256 JWT (`jose`, same library as staff auth), 30s expiry, `sub` = participant ID | Matches DESIGN.md CS3 ("rotating signed token, never raw member ID"). `apps/api/src/card/qr-token.ts`; verification will be reused by the scan-station app in Phase 4. |
| Rewards store points debit | Extended the ledger service's single-write-path rule to cover this, rather than writing `points_ledger`/`token_ledger` inserts in the rewards route | `redeemReward()` in `ledger-service.ts` debits points and (for `token_grant` rewards) credits tokens in one transaction — both or neither. `reward_redemptions` itself is a fulfillment record, not a ledger table, so the route inserts that row directly after the ledger call resolves. |
| Third-party client construction (Stripe, Clerk) | **Always lazy** (constructed inside the request handler on first use, cached in a module-level variable) — never `const stripe = new Stripe(...)` at module top level | Caught before shipping: `server.ts` imports every route module at startup, so a top-level `new Stripe(env.stripeSecretKey)` throws the instant the module loads if `STRIPE_SECRET_KEY` isn't set yet — which crashes the *entire* API (staff dashboard included), not just the Stripe-dependent routes. Verified by actually starting the server with those vars unset (simulating Railway's state before the keys are added) before pushing — see `apps/api/src/routes/checkout.ts`'s `getStripe()` pattern, mirrored in `webhooks-stripe.ts` and `auth/member-middleware.ts`'s `getClerkClient()`. Apply this pattern to any future third-party client. |
| Kiosk device identity (Phase 4) | Long-lived device JWT + short-lived staff-mode elevation JWT, both signed with their own `KIOSK_JWT_SECRET` (not `STAFF_JWT_SECRET`), stored client-side in `localStorage`/`sessionStorage` | The kiosk "user" is a single physically-secured tablet, not a per-person browser session — so unlike `apps/admin`/`apps/web`, `apps/scan-station` calls the API directly from client-side JS instead of proxying through Next.js Server Actions (the live camera QR-decode loop needs to). Staff-mode PIN unlock checks against `staff_users.kiosk_pin_hash` (a personal PIN per staff member, set via `PATCH /staff-users/:id`), not a device-wide PIN, so elevated actions are attributable to a specific person. |
| Scan resolution QR decoding | `jsqr` (pure JS, no native deps) reading frames off a `getUserMedia` camera stream into a canvas, decoded in a `requestAnimationFrame` loop | Simplest option that runs in any kiosk tablet browser with no native app / app-store distribution; DESIGN.md's kiosk↔space binding note already assumed a plain browser tablet, not a dedicated scanning peripheral. |
| Marketing homepage design direction (Phase 5) | **Superseded, see next row** — original direction was a dark, photo-forward hero + card grid (structure borrowed from mainevent.com/daveandbusters.com) reinterpreted in the brand's collegiate/varsity palette. Kept here for history. |
| Marketing homepage redesign — Apple + ESPN hybrid | **Light-mode Apple-style restraint** (huge whitespace, one 2-line headline, minimal copy, full-bleed hero photography carrying the message) **+ an ESPN-style dark "live scoreboard" strip** (`apps/marketing/app/components/LiveScoreboard.tsx`) rendering real `schedule_blocks` data as scannable tiles with a pulsing red live-dot — the one deliberately high-energy, dark band on an otherwise calm/light page | User rejected the first (dark, photo-card) direction as "not engaging," asked to research top sites (referenced Apple), then pivoted to ESPN specifically after rejecting a Stripe-inspired direction mid-request. Researched apple.com and espn.com (via WebFetch) before proposing this hybrid — Apple's pacing/restraint plus ESPN's live-data energy, since a generic "flashy" redesign wouldn't have been grounded in anything real. Removed the old `OpenNowTicker`/`StatsRow` components (marquee ticker + raw stat counters) in favor of the scoreboard, since Apple's restraint principle argues against a numbers-heavy stat row. Real facility photography still doesn't exist (see the row above and §5/§11) — the hero's full-bleed photo slot is now a bigger, more load-bearing gap than in the previous design, since there's nothing else on the page to distract from a placeholder; flagged as a harder blocker on this direction than the last one. |
| `apps/marketing` scope — real main site, not a landing page | Expanded from a single homepage to a real multi-page site: `/sports` (list) + `/sports/[slug]` (detail), `/leagues` (list) + `/leagues/[id]` (real standings), `/membership` (real token package pricing), `/about` (facility info + locations/contact) — all backed by new read-only `/public/*` endpoints (offerings, leagues, standings, token-packages), not static/hardcoded copy | User explicitly corrected the framing: "its not just a marketing site. its the main site for the alumni center." Confirmed scope with the user before building (data-driven vs static content, which pages) rather than assuming — landed on real live data throughout, since the backend already had nearly everything needed (sports/offerings/leagues/standings/token-packages all already existed for the staff/member apps; only the public read layer was new). The `/leagues/[id]/standings` endpoint reuses `rankStandings` from `@alumni/shared` — the exact same sport-specific ranking logic the staff dashboard uses — rather than re-deriving it, and deliberately returns only team name + record, never `captainParticipantId`. |
| `apps/marketing` visual overhaul — glassmorphism + real photography | Bigger logo (h-16 to h-24 depending on placement — was previously too small to read), a `ParallaxImage` client component (scroll-linked `translateY`) for the hero, `BlobBackground` (pure-CSS animated gradient blobs, no JS/images) for ambient motion, `.glass`/`.glass-light` CSS utilities (backdrop-blur + translucent bg) for overlapping "high-end app" panels, and real (if generic) stock photography from Lorem Picsum on every page instead of flat gradient color blocks. More CTAs throughout: a glass panel overlapping the hero into the next section, a "See all sports →" link above the offering grid, a gradient Alumni-Card panel with real starter-package pricing, and a "Ready to play?" CTA baked into the shared footer on every page. | User said the site was "bland," wanted it "more engaging and interactive," explicitly asked for bigger logo, free imagery, glassmorphism, animated/parallax backgrounds, and "high-end app" feel. Verified Lorem Picsum actually resolves to a real JPEG (`curl`'d both the redirect and the final asset) before using it, rather than guessing a photo-hosting URL — same caution as the color-block-placeholder decision earlier, just resolved differently now that a verified free image source exists. Caught and fixed a layout bug during review: a fixed `pt-20` spacer above `LiveScoreboard` left a large blank gap on days with no live schedule blocks (the component renders `null` when empty) — replaced with a fixed-height `h-24` spacer sized to only clear the hero's overlapping glass panel, independent of whether the scoreboard has content. |
| `apps/marketing` ESPN/Apple ticker + stats + hero logo | Added `TopTicker` (a slim scrolling marquee bar of real `open-now` schedule data, positioned directly under the header inside the hero — distinct from the fuller `LiveScoreboard` tiles further down) and brought back `StatsBand` (real count-up numbers from `/public/stats`, styled as big bold ESPN/Apple-style figures rather than the small muted row the first homepage draft had). Added a large centered logo lockup inside the hero itself (h-32/h-44), on top of the header's own bigger logo, so the mark reads as a focal element, not just corner branding. | User said the redesign was "better" but still wanted more explicit ESPN/Apple signal — "Tickers, scores, stats" — and that the logo still needed to be "bigger and more front and center" (not just bigger in the header corner). `StatsRow` had been deliberately removed earlier in favor of Apple's restraint-over-density principle (see the Apple+ESPN hybrid row above); reintroducing it here means both influences are represented simultaneously rather than picking one — restraint in typography/whitespace, but real scores/stats/tickers as explicit content blocks, since that's what the user asked for a second time. |
| Site photo management | **Real admin-editable image management**, not just a convention-based file swap | User asked "is there a way for me or in the admin panel to change the site photos?" and, offered both options, chose the fuller one. New `site_images` table (`slotKey` → `imageUrl`, upserted — never accumulates duplicate rows per slot), Vercel Blob for actual file storage (Vercel's production filesystem is read-only and `apps/api` runs on Railway anyway, so local disk was never an option), a staff-only upload route (`POST /site-images/:slotKey`, multipart via `@fastify/multipart`) mirroring the ledger's single-write-path convention, and a fully public read route (`GET /public/site-images`) apps/marketing polls with a Picsum fallback per slot. `apps/admin`'s new "Site Photos" page lists every known slot — the four static ones (hero/about/offering cards) plus one per row in the `sports` table, generated dynamically so a newly-added sport automatically gets its own upload slot with no code change. File uploads can't go through the existing `apiFetch` helper (it always JSON-stringifies the body) — `site-photos/actions.ts` has its own small Server Action forwarding raw `FormData` instead, still server-side with the same session-cookie bearer token. |
| Marketing site data | Fully unauthenticated `/public/*` API namespace (`apps/api/src/routes/public.ts`) — `open-now`, `stats`, `sports` | Kept separate from the staff/member schedule routes (which require auth) so the marketing site's live ticker/stat counters can call the API directly from client-side JS with zero risk of ever exposing participant/account identity — the route handlers only ever return aggregates and public schedule metadata. |

**Resolved:** `apps/admin`'s `next build` failed locally in this sandbox on Next's own auto-generated `/_global-error` fallback page (`Cannot read properties of null (reading 'useContext')` inside Next's internal `OuterLayoutRouter`) across every combination tried (Next 14.2.5/14.2.35/16.2.10, webpack/Turbopack, React 18/19, several pnpm linking modes, minimal-repro `layout.tsx`). The leading theory — this sandbox's non-LTS Node `v26.3.1`, outside Next's tested CI matrix — was **confirmed correct**: deploying to Vercel (`vercel deploy --prod` from `apps/admin`, project `play-on1/the-alumni-center-admin`) built and deployed successfully on the first try, no code changes needed. Live at `https://the-alumni-center-admin.vercel.app`. Lesson for future sessions: if `next build` behaves strangely in this specific sandbox again, suspect the Node version before the code — verify against a real deploy target early rather than spending a long time on local-only troubleshooting.

---

## 5. Assumptions made resolving DESIGN.md §5 open questions (flagging per kickoff instructions)

Phase 1 required no product-level open-question resolution (pure ledger/schema). Phase 2 required resolving four — all asked of the user before implementing, answers recorded in §4 above and DESIGN.md's own numbering: #1 (team formation → captain self-serve), #2 (score entry → staff only), #3 (standings formula → sport-specific), #6 (capacity/waitlists → enforced with waitlist). Phase 3 required two more, also asked before implementing: #27 (card photo → optional with avatar fallback) and #26 (Alumni Card cosmetics → full points-store unlock system, not a small default set — user's explicit call, overriding the recommendation). Remaining open questions (playoffs/brackets #4, free play pass tiers #7-8, most notification-provider questions #31) are deferred to the phases that actually need them.

Implementation-level defaults chosen without being asked (not product decisions, just fallbacks for unspecified sub-details — flagged here, not silently assumed):
- **Points earn rate default:** 1 point per token redeemed (`pointsEarnedForRedemption()` in `packages/shared/src/token-math.ts` defaults `ratePointsPerToken` to `1`), matching DESIGN.md's own example under open question #22 ("1 pt/token redeemed"). This is a parameter, not a hardcoded constant — callable with a different rate once question #22 is actually answered.
- **Schedule block overlap check is v1-simplified:** `apps/api/src/routes/schedule-blocks.ts` rejects overlaps by comparing each block's literal `starts_at`/`ends_at`, but does not expand `recurrence_rule` (RRULE) occurrences — two *recurring* weekly blocks whose instances would collide aren't caught. Acceptable for v1 (DESIGN.md open question #9, schedule admin UI scope); flagged here for whoever builds recurrence expansion later.
- **Reservation refund policy defaulted to always-refund-in-full** (DESIGN.md open question #5, still unresolved as a product decision): `POST /member/reservations/:id/cancel` always refunds 100% regardless of how close to the reservation time the cancellation happens. Needs a real cancellation-window policy before this goes near production — flagging here rather than blocking Phase 3 on it.
- **Split-payment reservations don't hold the slot against the DB exclusion constraint while pending.** The `reservations_no_overlap_per_space` constraint (migration `0001`) only blocks `booked`/`checked_in` rows — a `pending_split` reservation can theoretically race with another booking for the same slot before all shares are paid. DESIGN.md's own wording ("unpaid slot releases after a countdown") implies the slot *should* be held; v1 doesn't do this. Acceptable given split payments are a secondary path (most bookings are direct, single-payer), but a real fix (e.g. a short-lived advisory lock or including `pending_split` in the exclusion constraint) is needed before this is load-bearing for a busy facility.
- **`apps/marketing`'s offering cards use color-block placeholders, not real photography.** DESIGN.md doesn't address marketing site imagery at all; the user asked for a "fun and engaging" site with pictures, but no actual facility photos exist yet in this sandbox and hotlinking guessed stock-photo URLs risked broken/wrong images. Needs a real photo shoot (or licensed stock, done deliberately) before launch — see §11.

---

## 6. Repo structure & build order

```
/apps
  /marketing     → public website + landing page (Next.js, SSG) → Vercel        [Phase 5 — built]
  /web           → member PWA (Next.js 16 / React 19, Clerk auth) → Vercel        [Phase 3 — built]
  /admin         → staff dashboard (Next.js 16 / React 19) → Vercel              [Phase 2 — built]
  /scan-station  → kiosk/staff scan app (Next.js) → Vercel                       [Phase 4 — built]
  /api           → backend (Fastify) → Railway                                   [Phase 1 — built]
/packages
  /shared        → TypeScript types, token/points math, standings, validation   [Phase 1/2 — built]
  /db            → Drizzle schema + migrations                                  [Phase 1/2 — built]
/brand           → logo and brand assets
/docs            → DEPLOYMENT.md, HANDOFF.md
CLAUDE.md
README.md
.env.example
```

Build order (confirm with the user before starting each new phase):
1. **Ledger + API foundation** — done. Schema (31 tables), ledger service, tests, seed script.
2. **Admin dashboard** — done. Sport/space/offering/token-package/staff/vendor management, drag-and-drop schedule calendar, full league management (teams/games/scores/standings), member lookup + comps/refunds, staff phone+password auth.
3. **Member PWA** — done. Clerk phone-OTP auth (lazy account creation), Stripe token purchase (webhook-driven ledger credit), The Alumni Card (SVG + rotating QR + full cosmetics unlock system), browse/purchase offerings, reservations, split payments, "what's open now", PWA manifest + service worker.
4. **Scan-station app** — done. Kiosk + staff PIN modes, three-way scan resolution (free-play pass / enrollment check-in / walk-in with tap-to-confirm), vendor/concession POS mode, device-to-space binding.
5. **Marketing site** — done. Photo-forward homepage (real logo, card-style offering grid, live "what's open now" ticker, real ledger/account stats), varsity/collegiate brand direction — see §4 for the design decision history.
6. **Vendor/coach settlement + notifications** — Stripe Connect, settlement job, web push + SMS.

---

## 7. Ledger service — how to use it

Import from `apps/api/src/ledger/ledger-service.ts`. Every function runs inside its own DB transaction and takes a `Db` (from `@alumni/db`) as the first argument.

- `recordPurchase(db, { accountId, participantId, tokensGranted, bonusTokens?, stripePaymentIntentId?, ... })` — call only after Stripe webhook settlement, never client-side confirmation (not yet wired to Stripe — Phase 3).
- `recordRedemption(db, { accountId, participantId, amountTokens, beneficiary?, referenceType, referenceId, ... })` — checks balance, throws `InsufficientBalanceError` if insufficient, auto-earns points.
- `recordRefund(db, { ..., amountTokens, referenceType, referenceId, note, ... })` — new offsetting credit row, never mutates the original.
- `recordTransfer(db, { accountId, fromParticipantId, toParticipantId, amountTokens, ... })` — same-account only, throws `CrossAccountTransferError` otherwise.
- `recordAdjustment(db, { ..., amountTokens (signed), note (required), ... })` — staff corrections.
- `getParticipantBalance`, `getParticipantPointsBalance`, `getAccountTokenRollup` — read helpers, always derived from `SUM()`.
- `redeemReward(db, { participantId, pointsCost, tokenGrantAmount?, accountId?, referenceType, referenceId, note? })` — debits points; if `tokenGrantAmount` is set, credits tokens in the same transaction. Throws `InsufficientPointsBalanceError`.

**Do not** import `tokenLedger`/`pointsLedger` from `@alumni/db` anywhere else to `.insert()` into them. If a new feature needs to move tokens, add a function to `ledger-service.ts` or call an existing one.

Split payments (`apps/api/src/split-payments/split-payment-service.ts`) build on top of `recordRedemption`/`recordRefund` rather than writing ledger rows themselves — `createSplitRequest`, `acceptSplitShare` (each participant pays their own share; completing the last share flips the underlying reservation to `booked`), `declineSplitShare` (cancels the whole request), `expireStaleSplitRequests` (refunds any already-paid shares — no cron infra yet, call this periodically once Phase 6 background jobs exist).

---

## 8. Admin API surface + auth (Phase 2)

All routes except `/health` and `POST /auth/login` require `Authorization: Bearer <staff JWT>` (`requireStaffAuth`); mutating catalog/staff/partner routes additionally require `role=admin` (`requireAdminAuth`). See `apps/api/src/auth/middleware.ts`.

- `POST /auth/login`, `GET /auth/me`
- `GET/POST/PATCH /sports`, `/spaces`, `/token-packages`, `/partners`, `/staff-users` (admin-only), `/offerings` (+ `GET /offerings/:id`)
- `GET/POST/PATCH/DELETE /schedule-blocks` — `?spaceId=&from=&to=` filters; rejects overlaps (see §5 simplification note)
- `GET/POST /teams`, `/teams/:id/members`, `DELETE /teams/:id/members/:participantId`
- `GET/POST/PATCH /games`, `POST /games/:id/score` (staff-only, finalizing recalculates standings)
- `GET /offerings/:offeringId/standings` — always recomputed, never hand-edited
- `GET/POST /enrollments`, `POST /enrollments/:id/promote` (waitlist → enrolled, charges now), `POST /enrollments/:id/withdraw`
- `GET /members/search?q=`, `GET /members/:accountId`, `GET /participants/:participantId/ledger`
- `POST /participants/:participantId/comp` (ledger adjustment), `POST /participants/:participantId/refund` (ledger refund)
- `GET /site-images`, `POST /site-images/:slotKey` (multipart file upload → Vercel Blob, upserts the `site_images` row) — backs `apps/admin`'s "Site Photos" page, see §4/§11

`apps/admin` never calls these from client-side JS — every call is server-side (Server Component fetch or Server Action) using the staff JWT from an httpOnly cookie (`apps/admin/lib/session.ts`, `apps/admin/lib/api.ts`). The one exception in spirit, not mechanism: `POST /site-images/:slotKey` takes a file upload, which `lib/api.ts`'s `apiFetch` can't carry (it always JSON-stringifies the body) — `site-photos/actions.ts` has its own small Server Action that forwards the raw `FormData` instead, still server-side, still using the same session-cookie bearer token.

---

## 9. Member API surface + auth (Phase 3)

All routes below live under `/member/*` (e.g. `/member/me`) and require `Authorization: Bearer <Clerk session token>` (`requireMemberAuth`, `apps/api/src/auth/member-middleware.ts`), which lazily creates/links the caller's `accounts` row on first use (see §4).

- `GET /me` — account + participants with token/points balances; `POST /me/participants` (add a family member); `PATCH /me/participants/:id` (nickname, photo, card cosmetics config)
- `GET /participants/:id/qr-token` — signed, 30s-expiry QR payload; `GET /participants/:id/unlocked-cosmetics`
- `GET /token-packages`, `POST /token-packages/:id/checkout` → Stripe Checkout session URL
- `GET /offerings`, `POST /offerings/:id/purchase` — dispatches by offering type (enrollment for league/camp/lesson/clinic, a `passes` row for free_play_pass, immediate redemption for walk_in)
- `GET /schedule-blocks/open-now` — "what's open now" (same v1 simplification as staff schedule-blocks re: recurrence)
- `GET/POST /reservations`, `POST /reservations/:id/cancel`
- `GET /reward-items`, `POST /reward-items/:id/redeem`, `GET /participants/:participantId/reward-redemptions`
- `POST /split-requests`, `GET /split-requests/:id`, `POST /split-shares/:shareId/accept`, `POST /split-shares/:shareId/decline`

Unprefixed and separate: `POST /webhooks/stripe` — Stripe calls this directly (not under `/member`), verified against the raw request body via `fastify-raw-body` (`apps/api/src/routes/webhooks-stripe.ts`). This is the *only* place a token purchase credits the ledger — `POST /token-packages/:id/checkout` never does, since the client fully controls whether that redirect is ever hit.

---

## 10. Scan-station API surface + auth (Phase 4)

Two auth tiers, both distinct from the staff dashboard's JWT and the member PWA's Clerk session — see `apps/api/src/auth/kiosk-jwt.ts`/`kiosk-middleware.ts`.

- **Kiosk device token** (`requireKioskAuth`) — long-lived, issued once at device registration (`POST /kiosk-devices`, staff/admin JWT required, called from `apps/scan-station/app/register`). Signed with its own `KIOSK_JWT_SECRET`, deliberately separate from `STAFF_JWT_SECRET` so a stolen/compromised kiosk tablet's blast radius doesn't extend to the staff dashboard. Carries `deviceId` + the bound `spaceId`.
- **Kiosk staff-mode token** (`requireKioskStaffAuth`) — 15-minute expiry, issued by `POST /scan-station/pin-unlock` after a correct PIN match against a staff user's `kioskPinHash` (`staff_users.kiosk_pin_hash`, set via `PATCH /staff-users/:id` with `{ kioskPin }` — a personal PIN per staff member, not a device-wide one, so staff-mode actions are attributable to a specific person for audit purposes).

Device management (staff/admin JWT):
- `GET/POST/PATCH/DELETE /kiosk-devices`, `POST /kiosk-devices/:id/rotate-token` — issuing a fresh token doesn't invalidate the old one by itself (JWTs aren't checked against the DB per-request); pair a compromised-device response with rotating `KIOSK_JWT_SECRET`, not just deleting the row.

Kiosk-authenticated (`requireKioskAuth`):
- `POST /scan-station/pin-unlock` — body `{ pin }`, returns a staff-mode token + the matched staff user's name
- `GET /scan-station/schedule` — current + next ~4h of schedule blocks for the kiosk's bound space (poll every 30-60s, same v1 simplification as staff/member schedule-blocks re: recurrence)
- `POST /scan-station/scan` — body `{ qrToken }` (from the member's Alumni Card, verified via the same `qr-token.ts` the card page uses). Three-way resolution per DESIGN.md: active free-play pass covering this space right now → `pass_checkin` (activates an unused pass on first scan), no deduction; enrolled in the league/camp/lesson scheduled here right now → `enrollment_checkin`, no deduction; neither → `walk_in_available`, offering a token price but charging nothing yet. `denied` if the space has no open block right now. See `apps/api/src/scan/scan-service.ts`.
- `POST /scan-station/confirm-walk-in` — the explicit tap-to-confirm step after `walk_in_available`; this is the only path that actually calls `recordRedemption` for a scan. Kiosk mode never auto-charges off a scan alone.
- `GET /scan-station/menu-items` — read-only menu catalog for the vendor POS screen
- `POST /scan-station/vendor-orders` — body `{ qrToken, accountId, partnerId, items: [{ menuItemId, qty }] }`; debits the house beneficiary if `partnerId` is null, otherwise the named vendor partner (settlement-by-beneficiary picks this up via the existing `token_ledger.beneficiary_partner_id` column, no new mechanism needed). See `apps/api/src/vendor-orders/vendor-order-service.ts`.

Kiosk staff-mode-authenticated (`requireKioskStaffAuth`):
- `GET /scan-station/member/:participantId` — manual lookup (name, photo, balance) for the "does this photo match" anti-fraud check DESIGN.md calls out, and as a fallback when a member's phone/QR isn't handy.

Staff/admin JWT (shared with the admin dashboard's namespace, unprefixed):
- `GET/POST/PATCH /menu-items` — concession/vendor item catalog management.

**Architectural note:** unlike `apps/admin`/`apps/web`, `apps/scan-station` stores its device token in the browser's `localStorage` and calls the API directly from client-side JS (`apps/scan-station/lib/api.ts`) rather than proxying every request through Next.js Server Actions. This is a deliberate deviation, not an oversight: the kiosk "user" is a single physically-secured tablet running a live camera-decode loop, not a multi-tenant browser session that needs its token hidden from other page JS the way a staff/member's JWT does in the other two apps.

---

## 11. Public API surface (`apps/marketing`, Phase 5)

`apps/api/src/routes/public.ts` — no auth, no auth middleware at all, registered before every other route group. `apps/marketing` is **the real main site**, not a single landing page — see the "main site, not a landing page" row in §4 — with real pages for sports/leagues/membership/about, all pulling this data. Most calls happen server-side in React Server Components (`apps/marketing/lib/api.ts`'s functions are called directly from `async function Page()` components, using Next's `fetch` cache/`revalidate`); the two genuinely real-time pieces (`LiveScoreboard`, and any future live counter) are client components polling on an interval. Either way there's no session to protect — nothing here is behind client-vs-server-fetch security, it's just a performance/freshness choice (ISR-style `revalidate: 60` for catalog-ish reads, `no-store` for the live scoreboard).

- `GET /public/open-now` — current + next ~4h of schedule blocks across all spaces (space name, sport name, mode, times) — powers the homepage's "live scoreboard" strip (`apps/marketing/app/components/LiveScoreboard.tsx`, née the ticker — see §4's redesign row). No participant or account data.
- `GET /public/stats` — three aggregates: `activeMembers`, `tokensInPlay` (`SUM(token_ledger.amount)` — total unspent tokens currently held across every participant, not a per-person balance), `leaguesRunning`. Not currently rendered anywhere (the redesigned homepage dropped the raw stat row per Apple's restraint principle) — kept in the API since it's cheap and a future page may want it.
- `GET /public/sports` — active sports list (name/slug/icon) — backs `/sports` and `/sports/[slug]`.
- `GET /public/offerings?sportSlug=&type=` — active offerings (walk-ins, camps, lessons, etc.), optionally filtered by sport/type — backs `/sports/[slug]`'s per-sport offering list.
- `GET /public/leagues` — active `league`-type offerings — backs `/leagues`.
- `GET /public/leagues/:offeringId/standings` — real, sport-specific-ranked standings (reuses `rankStandings` from `@alumni/shared`, the same function the staff-only `GET /offerings/:offeringId/standings` uses) joined with team names — backs `/leagues/[id]`. Deliberately returns only `teamName` + win/loss/tie/points record, never `captainParticipantId` or anything participant-linked, even though the source `teams` row has that column.
- `GET /public/token-packages` — active token packages (name/price/tokens/bonus) — backs `/membership`'s real pricing.
- `GET /public/site-images` — `{ slotKey: imageUrl }` map of every real photo staff has uploaded via `apps/admin`'s "Site Photos" page (see §4/§8 and `packages/db/src/schema.ts`'s `site_images` table). Empty for any slot nothing's been uploaded to yet — `apps/marketing/lib/images.ts`'s `resolveImage()` falls back to a Picsum placeholder in that case, per slot.

**Real facility photography does not exist yet**, but the site is no longer stuck with hardcoded placeholder URLs: staff can upload real photos anytime via `apps/admin`'s "Site Photos" page (backed by Vercel Blob storage + the `site_images` table, §4/§8 above), and every photo slot on the site checks for a real uploaded photo before falling back to [Lorem Picsum](https://picsum.photos) (a real, stable, long-running free stock-photo placeholder service, verified to actually resolve before using it — not a guessed/hotlinked one-off URL). **The `/about` page's address/hours/phone/email are still separately explicit `[ placeholder ]` bracketed text**, not fabricated real-looking contact info, since there's no equivalent "upload real facility details" UI yet.

---

## 12. Testing & local dev

- `apps/api` tests run against a real Postgres database (`TEST_DATABASE_URL`), not mocks — the ledger's correctness depends on real transactional/locking behavior that mocks can't verify.
- `beforeEach` truncates all tables in the test DB. This is a test-harness concern only; it does not violate the "ledger rows are never updated/deleted" production invariant.
- `apps/api`'s `vitest.config.ts` sets `fileParallelism: false` — all test files share one physical test DB and isolate via truncate; running files in parallel races those truncates (caused real, confusing failures during Phase 2 until this was set — see changelog).
- See `README.md` for full local setup, including running `apps/admin` alongside `apps/api`.

---

## 13. Changelog

| Date | Change |
|---|---|
| 2026-07-03 | Phase 1 complete: monorepo skeleton, `CLAUDE.md`/`README.md`/`.env.example` bootstrapped, full 31-table schema migrated (Drizzle), token ledger service with single-write-path invariant + advisory-lock concurrency safety, 9 passing ledger tests against a real Postgres DB, seed script (sport/space/schedule block/token package/staff user/test account with 2 participants, funded via the real ledger path) |
| 2026-07-03 | Pushed to GitHub (`garcpaul10/thealumnicenter`). Deployed `apps/api` + Postgres to Railway (project `the-alumni-center`), fully via CLI — no dashboard clicks, config lives in `railway.json`. Fixed a real production bug caught before shipping: `apps/api`'s plain-`tsc` build produced a `dist/server.js` that couldn't import `@alumni/db`/`@alumni/shared` at runtime (those packages ship raw TS source, no dist of their own) — switched to `tsup` bundling those two workspace packages in (`apps/api/tsup.config.ts`). Migrations run successfully against the live Railway Postgres (31 tables confirmed). Vercel intentionally deferred until Phase 2 produces an actual frontend app to deploy. |
| 2026-07-03 | **Phase 2 complete: admin dashboard.** Resolved 4 product open questions with the user (team formation, score entry, standings formula, capacity/waitlist — see §4/§5). Backend: staff phone+password auth (JWT), full CRUD for sports/spaces/schedule-blocks/offerings/token-packages/partners/staff-users, league management (teams/games/scores/sport-specific standings, recalculated on finalize), enrollments with capacity+waitlist, member lookup + comps/refunds via the existing ledger service, 15 new tests (18 total in apps/api, 33 across the workspace). Added `staff_users.password_hash` (migration `0002_fat_vermin.sql`). Fixed a real transaction-atomicity gap caught before merging: `createEnrollment` was calling `recordRedemption` and inserting the enrollment row as two separate DB calls — wrapped both in one outer transaction. Frontend: `apps/admin` (Next.js 16.2.10 + React 19 + Tailwind), login, full nav, all management pages, and a custom drag-and-drop schedule calendar (`@dnd-kit/core`, courts×time grid). |
| 2026-07-03 | Deployed `apps/admin` to Vercel (project `play-on1/the-alumni-center-admin`, live at `https://the-alumni-center-admin.vercel.app`) — succeeded on the first try, confirming the `next build` failure hit during local development was specific to this sandbox's non-LTS Node version, not the code. See §4 for the resolved troubleshooting history. |
| 2026-07-04 | Live-deploy hygiene fix: Railway's `api` service was missing migration `0002` (`staff_users.password_hash`) and the `STAFF_JWT_SECRET` env var — both landed in Phase 2 commits but were never pushed to the live service, silently breaking the deployed admin login while every local/test environment looked fine. Fixed (migration applied, secret set, redeployed) and added an explicit "this is ongoing maintenance, not one-time setup" callout to `docs/DEPLOYMENT.md` so new migrations/env vars get pushed to Railway as a standard part of landing them, not as an afterthought. |
| 2026-07-04 | **Phase 3 complete: member PWA.** Resolved 2 more product open questions with the user before implementing (#27 card photo → optional/avatar fallback, #26 card cosmetics → full points-store unlock system, not a small default set). Backend: Clerk-authenticated `/member/*` API surface (lazy account/participant creation), Stripe Checkout + webhook-driven token purchase (ledger credit only on verified webhook, never client redirect), rotating signed QR tokens, rewards store (`redeemReward()` added to the ledger service — points debit + optional token credit in one transaction), member-facing offering purchase (walk-in/free-play/enrollment dispatch), reservations with cancel/refund, and full split-payment lifecycle (create/accept/decline/expire-with-refund). 8 new tests (26 total in apps/api). Added `accounts.clerk_user_id` (migration `0003`). Frontend: `apps/web` (Next.js 16/React 19/Tailwind, Clerk phone sign-in) — wallet, Alumni Card (SVG + live-rotating QR via a same-origin proxy route so the Clerk token never reaches client JS), browse/purchase, rewards store, PWA manifest + service worker. Flagged two real gaps rather than silently shipping them: reservation refund policy defaults to always-refund-in-full (open question #5 still unresolved), and split-payment reservations don't yet hold their slot against the DB exclusion constraint while pending (see §5). Caught and fixed a real deploy-breaking bug before pushing: `checkout.ts`/`webhooks-stripe.ts`/`member-middleware.ts` originally constructed the Stripe/Clerk clients at module load time, which would have crashed the *entire* live API (including the already-working staff dashboard) the moment this deployed, since Railway doesn't have Stripe/Clerk keys set yet — switched to lazy construction and verified by actually starting the server locally with those vars unset before pushing. Also found `fastify-raw-body@5.x` requires Fastify 5 while this project is on Fastify 4 — pinned to `^4.3.0` instead; also caught via the same local no-keys smoke test, not by code review. |
| 2026-07-04 | **Phase 3 cleanup complete.** Added a real `apps/web` sign-up page (`app/sign-up/[[...sign-up]]/`) — Phase 3 had shipped only `/sign-in`, so new members had no way to create an account; found via manually testing the actual sign-up flow in a browser, not code review. Fixed the Clerk instance's dashboard config (phone-OTP was accidentally requiring a password too — a dashboard setting, not code, documented in `docs/DEPLOYMENT.md`). Set real Stripe test-mode keys and Clerk keys (previously only placeholders); created the production Stripe webhook endpoint via the Stripe API directly (`POST /v1/webhook_endpoints`) pointed at the live Railway API, subscribed to `checkout.session.completed`. Set `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, `QR_SIGNING_SECRET` on Railway's `api` service and redeployed — verified staff login still works post-deploy each time (the lazy-client-construction fix from the last Phase 3 commit held up). Deployed `apps/web` to Vercel (project `play-on1/the-alumni-center-web`, live at `https://the-alumni-center-web.vercel.app`) with its own env vars. Updated `ADMIN_APP_ORIGIN`/`WEB_APP_ORIGIN` on Railway to the real Vercel URLs for CORS. `docs/DEPLOYMENT.md` and `docs/HANDOFF.md` updated to reflect all of the above as actually-live infrastructure, not placeholders. |
| 2026-07-05 | **Phase 4 complete: scan-station app.** Backend: kiosk device auth (own `KIOSK_JWT_SECRET`, separate from staff dashboard's) plus a short-lived staff-mode elevation token gated on a per-staff-member `kiosk_pin_hash`; device registration/space-binding CRUD (`/kiosk-devices`); three-way scan resolution (`apps/api/src/scan/scan-service.ts`) — active free-play pass or league/camp enrollment check in with no charge, anything else offers a walk-in that only actually deducts tokens after an explicit tap-to-confirm (`POST /scan-station/confirm-walk-in`), never off the scan itself; vendor/concession POS (`apps/api/src/vendor-orders/vendor-order-service.ts`) debiting the house or a named vendor beneficiary. 10 new tests (36 total in apps/api). Caught and fixed a real bug before it shipped: the vendor order service was passing a non-UUID value (`"house"` or a nullable partner id) into `token_ledger.reference_id`, a UUID column — fixed by inserting the `vendor_orders` row first and using its own id as the ledger reference, caught by the new tests rather than code review. Frontend: `apps/scan-station` (Next.js 16/React 19/Tailwind) — one-time device registration flow (staff login → pick space → device token stored in browser localStorage), a live camera QR-scan kiosk screen (`jsqr` + `getUserMedia`, no native app needed), a staff-mode PIN pad, manual member lookup (photo/balance, for the anti-fraud "does this match" check), and a vendor POS screen. Architectural deviation from `apps/admin`/`apps/web`, documented in §4/§10: this app calls the API directly from client-side JS with a localStorage-held device token, since the kiosk "user" is a single secured tablet running a live camera loop, not a per-person session needing its token hidden from other page JS. Verified end-to-end in a real browser (register → PIN unlock → staff lookup → vendor POS all worked against the live local API + seed data), plus all 36 automated tests. Deployed `apps/scan-station` to Vercel (project `play-on1/the-alumni-center-scan-station`, live at `https://the-alumni-center-scan-station.vercel.app`); set `KIOSK_JWT_SECRET` and `SCAN_STATION_APP_ORIGIN` on Railway's `api` service (CORS) and redeployed. `docs/DEPLOYMENT.md`/`docs/HANDOFF.md`/`README.md` updated to match. |
| 2026-07-05 | **Phase 5 complete: marketing site.** Discussed design direction with the user before building — referenced mainevent.com/daveandbusters.com for structure, iterated through three mockups (flat/subdued → motion/hover → photo-forward cards) after the first was rejected as "bland" and the user asked for something engaging with animations, real pictures, and the real logo. Landed on: photo-forward hero, card-style offering grid, live "what's open now" ticker, and real (not fake) stat counters — animated on scroll but sourced from the actual ledger/accounts tables. Backend: new fully unauthenticated `/public/*` namespace (`apps/api/src/routes/public.ts` — `open-now`, `stats`, `sports`), deliberately separate from the staff/member schedule routes so it can never expose participant/account identity. Frontend: `apps/marketing` (Next.js 16/React 19/Tailwind, SSG) — real logo (`brand/alumni-center-logo.png` copied into `public/`), animated star motif, marquee ticker, count-up stat animation, hover-lift offering cards. Flagged a real, deliberate gap rather than silently working around it: no facility photography exists yet, so offering cards use gradient-tinted color blocks instead of guessed/hotlinked stock photo URLs (which could 404 or resolve to the wrong image) — a real photo shoot or licensed stock is needed before launch, documented in `CLAUDE.md` §5/§11. Verified end-to-end in a real browser against the live local API (real stat numbers rendered correctly: 2 active members, 65 tokens in play, matching seed data) — no console errors. Deployed to Vercel (project `play-on1/the-alumni-center-marketing`, live at `https://the-alumni-center-marketing.vercel.app`); set `MARKETING_APP_ORIGIN` on Railway's `api` service (CORS) and redeployed. `docs/DEPLOYMENT.md`/`docs/HANDOFF.md`/`README.md` updated to match. All five build-order phases are now complete. |
| 2026-07-05 | **Marketing homepage redesign: Apple + ESPN hybrid.** User called the Phase 5 homepage "not engaging," asked to research top sites — referenced Apple explicitly, then pivoted mid-conversation from a Stripe-inspired follow-up to ESPN specifically. Researched apple.com and espn.com via WebFetch (not from memory) before proposing a hybrid direction: Apple's pacing/restraint (huge whitespace, one 2-line headline, minimal copy, full-bleed photography) plus an ESPN-style dark "live scoreboard" strip for real schedule data (red pulse-dot, scannable tiles) — the one deliberately high-energy band on an otherwise calm, light page. Flipped the whole site from dark to light mode. Removed `OpenNowTicker` (marquee) and `StatsRow` (count-up numbers) in favor of the new `LiveScoreboard` component — Apple's restraint principle argues against a numbers-heavy stat row, and the scoreboard reuses the same `GET /public/open-now` data more legibly. Rewrote `page.tsx` and `OfferingCards.tsx` for the light palette. Verified in a real browser: created a temporary live `schedule_blocks` row via the API to confirm the scoreboard actually renders real data correctly (not just its empty state), screenshotted it, then deleted the test row — no console errors. Flagged that this direction raises the stakes on the still-missing real facility photography (see §4/§5/§11): with far less other content on the page, a placeholder photo block is more exposed than in the previous design. |
| 2026-07-05 | **`apps/marketing` expanded into a real multi-page main site.** User corrected the framing: "its not just a marketing site. its the main site." Asked two clarifying questions before building (real live data vs static content; which pages) rather than assuming scope — user chose real live data and all of Sports/Leagues/Membership/Locations+About. Backend: four new `/public/*` endpoints (`GET /public/offerings`, `GET /public/leagues`, `GET /public/leagues/:offeringId/standings`, `GET /public/token-packages`) — the standings endpoint reuses `rankStandings` from `@alumni/shared`, the same sport-specific ranking logic the staff dashboard's `GET /offerings/:offeringId/standings` uses, and deliberately strips everything except team name + win/loss/tie record. Frontend: `/sports` + `/sports/[slug]`, `/leagues` + `/leagues/[id]`, `/membership` (real token package pricing), `/about` (facility info — address/hours/phone are explicit bracketed placeholders, not fabricated real-looking contact info). Extracted `SiteHeader`/`SiteFooter` shared across all pages, with real nav links replacing the static text labels. Verified every new page against the live local API and seed data in a real browser (real sport offerings, real $50/50-token package pricing, correct empty states for leagues with no active seasons) — no console errors, all 36 API tests still passing. |
| 2026-07-05 | **`apps/marketing` visual overhaul: glassmorphism, real photography, bigger logo.** User called the site "bland," wanted it "way more engaging and interactive," and explicitly asked for a bigger logo, free imagery, glassmorphism, animated/parallax backgrounds, more CTAs, and a "high-end app" feel. Verified Lorem Picsum actually resolves to a real image (`curl`'d it) before adopting it as the photo source, rather than guessing/hotlinking. Added: `ParallaxImage` (scroll-linked hero), `BlobBackground` (pure-CSS animated gradient blobs, light/dark variants), `.glass`/`.glass-light` utilities, a bigger logo across every page (`SiteHeader` now has `overlay`/`light` variants), real photo cards on the homepage/sports pages/sport-detail/about (replacing flat color-block placeholders), a "MOST POPULAR" featured membership tier, and more CTAs throughout (overlapping hero panel, "See all sports →", gradient Alumni-Card panel with real pricing, footer CTA on every page). Caught and fixed a real layout bug during review, not after shipping: a fixed spacer above `LiveScoreboard` left a large blank gap whenever there's no live schedule block (the component renders nothing) — fixed by sizing the spacer to the hero panel's overlap alone, independent of the scoreboard's content. Verified in a real browser across the homepage, a sport detail page, and the membership page — no console errors. |
| 2026-07-05 | **`apps/marketing`: ESPN/Apple ticker, stats, and a front-and-center logo.** User said the redesign was better but still wanted more explicit "ESPN/Apple feel" — tickers, scores, stats — and that the logo needed to be bigger and more front-and-center, not just a bigger corner mark. Added `TopTicker` (scrolling marquee of real `open-now` schedule data, directly under the header) and `StatsBand` (real count-up `activeMembers`/`tokensInPlay`/`leaguesRunning` numbers, big and bold, flowing into the same dark band as `LiveScoreboard`) — reinstating the stat-counter concept dropped during the earlier Apple-restraint redesign, now deliberately alongside the restraint principle rather than instead of it. Added a large centered logo lockup inside the hero (on top of the header's own logo) so the mark is a focal hero element. Verified end-to-end in a real browser by temporarily creating a live `schedule_blocks` row via the API (confirmed the ticker and scoreboard both render real data, not just empty states) and confirming the stats band shows the actual seeded numbers (2 members, 65 tokens, 0 leagues) — then deleted the test row. No console errors. |
| 2026-07-06 | **Real site-photo management: apps/admin "Site Photos" page + Vercel Blob.** User asked whether photos could be changed from the admin panel; offered a cheap convention-based-file-swap option vs. a real admin-editable upload system, and the user chose the latter. Added a new `site_images` table (migration `0004`), a staff-only upload route (`POST /site-images/:slotKey`, `@fastify/multipart` + `@vercel/blob`) as the single write path for that table, and a fully public `GET /public/site-images` read route. 3 new tests for the upsert-by-slot service logic (39 total in apps/api). Frontend: `apps/admin`'s new "Site Photos" page (8 slots: hero, about, 4 offering cards, one per row in `sports` — dynamically generated, so a new sport automatically gets its own upload slot) with a small dedicated Server Action for the file upload, since the existing `apiFetch` helper always JSON-stringifies its body and would have corrupted binary image data. `apps/marketing` now checks for a real uploaded photo per slot before falling back to Picsum (`lib/images.ts`'s `resolveImage()`), across the homepage, offering cards, sports list/detail, and about page. Created the real Vercel Blob store (`the-alumni-center-photos`, public access) via `vercel blob create-store`, and — asked "can't you do this?" after initially flagging the store-to-project connection as a dashboard-only manual step — found the actual REST endpoint the dashboard's "Connect Store" button calls (`POST /v1/storage/stores/:id/connections`, discovered by grepping the installed `vercel` CLI's own bundled source for its usage, not guessed) and completed the connection via `curl` with the CLI's own stored auth token, no dashboard click needed after all. Pulled the resulting `BLOB_READ_WRITE_TOKEN` and set it on Railway's `api` service and local `.env`. Verified with a real end-to-end upload (`POST /site-images/hero` with an actual JPEG) that produced a real, publicly-resolvable Blob URL, confirmed it via `GET /public/site-images` and a direct fetch of the URL, then deleted the test blob and its `site_images` row. `docs/DEPLOYMENT.md` updated to document the working `curl`-based connection method for handoff, replacing the earlier "manual dashboard step" framing. |
