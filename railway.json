{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @alumni/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @alumni/api start"
  }
}
